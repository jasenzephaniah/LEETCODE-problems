class Solution:
    def jump(self, nums: List[int]) -> int:
        m = 0
        i = 0
        j = 0
        e = 0
        while i < len(nums) - 1:
            m = max(m, i + nums[i])
            if i == e:
                j += 1
                e = m
            i += 1
        return j
